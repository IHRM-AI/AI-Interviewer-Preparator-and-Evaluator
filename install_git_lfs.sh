#!/bin/bash
# Git LFS installation script for macOS
# Option 1: Using Homebrew (recommended)
# Run: brew install git-lfs

# Option 2: Manual download
# Download from: https://github.com/git-lfs/git-lfs/releases
# Then run: ./git-lfs install

echo "To install Git LFS:"
echo "1. Install Homebrew if not installed: /bin/bash -c \"\$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\""
echo "2. Then run: brew install git-lfs"
echo "3. Then run: git lfs install"
